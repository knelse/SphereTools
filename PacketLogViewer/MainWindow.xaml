<Window x:Class="PacketLogViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PacketLogViewer"
        mc:Ignorable="d"
        Title="PacketLogViewer"
        Height="1440"
        Width="2560">
    <Window.Resources>
        <Style x:Key="LogRowHighlightStyle" TargetType="ListViewItem">
            <Style.Setters>
                <Setter Property="FontFamily" Value="Hack"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Height" Value="20"/>
            </Style.Setters>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Origin}" Value="SRV">
                    <Setter Property="Background" Value="Beige"></Setter>
                    <Setter Property="Foreground" Value="Black" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Origin}" Value="CLI">
                    <Setter Property="Background" Value="AliceBlue"></Setter>
                    <Setter Property="Foreground" Value="Black" />
                </DataTrigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="MidnightBlue" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="LogRecordTextViewStyle" TargetType="TextBox">
            <Style.Setters>
                <Setter Property="FontFamily" Value="Hack"/>
                <Setter Property="FontSize" Value="12"/>
            </Style.Setters>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <GridSplitter Grid.Column="0" Width="8" ResizeBehavior="CurrentAndNext" Margin="10 4 0 4 "/>
    <ListView Margin="4" Name="LogList" Grid.Column="0" ItemContainerStyle="{StaticResource LogRowHighlightStyle}" SelectionMode="Single">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Origin" Width="Auto" DisplayMemberBinding="{Binding Origin}" />
                <GridViewColumn Header="Date" Width="Auto" DisplayMemberBinding="{Binding Date, StringFormat='{}{0:dd.MM.yy HH:mm:ss}'}"/>
                <GridViewColumn Header="Content" Width="Auto" DisplayMemberBinding="{Binding Content}"/>
            </GridView>
        </ListView.View>
    </ListView>
    <TextBox Name="LogRecordTextDisplay" Grid.Column="1" Margin="4" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Style="{StaticResource LogRecordTextViewStyle}">
        aaabbbbbbbbbcccccccccccccc
    </TextBox>
    </Grid>
</Window>